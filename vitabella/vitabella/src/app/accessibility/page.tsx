import AccessibilityClient from './AccessibilityClient';

export const metadata = {
  title: 'Accessibility | Vita Bella',
  description: 'Learn about Vita Bella’s commitment to accessibility and the features we provide to ensure our website is usable by everyone.',
  alternates: { canonical: '/accessibility' },
};

export default function AccessibilityPage() {
  return <AccessibilityClient />;
}
